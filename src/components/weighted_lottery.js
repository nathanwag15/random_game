import React, { Component } from 'react';

export default function(props){
    var length =  this.props.length;
    // keys = [];
    // items =  [];

    return(
        <div>
            {length}
        </div>
    )
}